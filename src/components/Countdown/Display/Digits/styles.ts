import styled from "styled-components";
import DigitsProps from "./types";

const StyledDigits = styled.div<DigitsProps>`
  display: flex;
  align-items: center;
  justify-content: space-around;
`

export default StyledDigits